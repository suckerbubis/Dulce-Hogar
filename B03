<!doctype html>
<html lang="es">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Dulce Hogar — B-03 Recuperar contraseña</title>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&family=Source+Code+Pro&display=swap" rel="stylesheet">
<style>
  :root{
    --bg:#0c0f14;
    --grid:#1b2536;
    --panel:#0f1520;
    --stroke:#e6e6e6;
    --text:#e9edf5;
    --muted:#b9c1d1;
    --red:#f43f5e;
  }
  *{box-sizing:border-box}
  body{
    margin:0; background:var(--bg); color:var(--text);
    font-family:Inter,system-ui,Segoe UI,Roboto,Helvetica,Arial,sans-serif;
    min-height:100vh; display:grid; place-items:center; padding:20px;
  }
  .stage{
    position:relative; width:1100px; height:650px; border:1px solid #22304a; border-radius:10px;
    background:
      linear-gradient(var(--grid) 1px, transparent 1px) 0 0/32px 32px,
      linear-gradient(90deg, var(--grid) 1px, transparent 1px) 0 0/32px 32px,
      var(--panel);
    overflow:hidden; padding:24px;
  }

  /* ----- Bloques ----- */
  .box{
    position:absolute; background:transparent; color:var(--text);
    border:2px solid var(--stroke); border-radius:8px; padding:12px;
  }
  .title{font-weight:700; margin-bottom:10px; letter-spacing:.2px}
  .label{font-size:13px; color:var(--muted); margin-bottom:6px}
  .input{
    width:100%; height:36px; background:transparent; border:2px solid var(--stroke);
    border-radius:6px; color:var(--text); padding:0 10px; margin-bottom:10px; outline:none;
  }
  .btn{
    display:inline-block; padding:8px 12px; border:2px solid var(--stroke);
    color:var(--text); background:transparent; border-radius:6px; font-weight:700;
  }
  .dialog .body{font-size:14px; margin:8px 0 10px}
  .dialog .ok{display:inline-block; padding:6px 12px; border:2px solid var(--stroke); border-radius:6px}

  /* API card */
  .api{font-family:"Source Code Pro", ui-monospace, monospace; font-size:13px}
  .api .sig{margin-top:6px}

  /* Flechas rojas (SVG overlay) */
  svg.lines{position:absolute; inset:0; pointer-events:none}
  .arrow{stroke:var(--red); stroke-width:3; fill:none; marker-end:url(#ah)}

  /* Puntos azules decorativos como en tu captura */
  .dot{
    position:absolute; right:10px; top:10px; width:12px; height:12px;
    border-radius:50%; border:2px solid #9ec8ff;
  }

  /* Posiciones aproximadas para coincidir con tu imagen */
  #form   {left:60px;  top:70px;  width:360px}
  #okDlg  {left:520px; top:90px;  width:260px}
  #expDlg {left:520px; top:290px; width:280px}
  #apiBox {left:90px;  top:380px; width:300px}
</style>
</head>
<body>
  <div class="stage">
    <!-- Formulario -->
    <div class="box" id="form">
      <div class="title">Recuperar contraseña</div>
      <div class="label">correo electrónico</div>
      <input class="input" type="email" placeholder="usuario@correo.com">
      <button class="btn">Enviar enlace de recuperación</button>
    </div>

    <!-- Diálogo Éxito -->
    <div class="box dialog" id="okDlg">
      <div class="dot" aria-hidden="true"></div>
      <div class="title">Éxito</div>
      <div class="body">Se ha enviado un enlace a su correo</div>
      <div class="ok">OK</div>
    </div>

    <!-- Diálogo Token caducado -->
    <div class="box dialog" id="expDlg">
      <div class="title">Token caducado</div>
      <div class="body">
        Dado que intento usar un token después de 15 minutos
      </div>
      <div class="ok">OK</div>
    </div>

    <!-- API -->
    <div class="box api" id="apiBox">
      <div class="title">API::recuperar</div>
      <div>+ email : String</div>
      <div class="sig">→ Response : Token : String</div>
    </div>

    <!-- Conexiones (flechas rojas) -->
    <svg class="lines" viewBox="0 0 1100 650" preserveAspectRatio="none">
      <defs>
        <marker id="ah" markerWidth="12" markerHeight="8" refX="10" refY="4" orient="auto">
          <polygon points="0,0 12,4 0,8" fill="#f43f5e"/>
        </marker>
      </defs>
      <!-- form -> éxito -->
      <path class="arrow" d="M 420 140 C 470 140, 470 130, 520 130"/>
      <!-- éxito -> token caducado (vertical) -->
      <path class="arrow" d="M 650 180 C 650 220, 660 240, 660 290"/>
      <!-- form -> API -->
      <path class="arrow" d="M 280 230 C 280 300, 280 330, 240 380"/>
    </svg>
  </div>
</body>
</html>
